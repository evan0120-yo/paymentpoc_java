# 交接文件

## 閱讀方式

- BLOCK 1: 先用產品與架構角度看整體，適合先建立全貌。
- BLOCK 2: 用非技術版方式說明這個 POC 裡的重要概念，適合快速交接。
- BLOCK 3: 對照目前程式碼做詳細說明，適合接手後實際修改與延伸。

---

## BLOCK 1: 系統簡介與整體架構

### 1. 這個 POC 是什麼

這是一個印度市場的支付聚合系統 POC，當前場景聚焦在 biller 方向，也就是「帳單/充值類型」的支付流程。

公司本身沒有電子支付執照，所以系統定位不是資金持有方，而是支付聚合商。比較容易理解的類比，是早期沒有電支執照時期的 LINE Pay 角色: 自己不直接持有支付牌照，而是整合上游支付能力、統一流程、統一事件、統一資料模型，對內提供可控的支付編排層。

這份 POC 撰寫時，主要只考慮到 biller 方向，因此目前重點是:

- 如何把一筆 bill payment / recharge 類交易導入上游支付渠道。
- 如何把 Payin callback 統一收口。
- 如何讓 PayCore 成為總狀態機與 Saga 協調中心。
- 如何把後續的 Biller / PayLedger 顯示層，從核心交易流程切開。

這份 POC 不是完整支付平台，至少目前還不是。它目前比較像是:

- 有核心骨架。
- 有模組切分。
- 有事件流。
- 有 Outbox 與補償思路。
- 但外部介接與 read side 還多半是殼或 mock。

### 2. 這個 POC 目前不包含什麼

- 目前只有 PA，沒有 PG。
- 因為只有 PA，支付資料不會進到我方 server，所以目前沒有 PCI DSS 規劃。
- 目前沒有真的把 user 的 bill 幫忙繳掉，也就是 payout / payment 半段還沒有完成。
- 目前沒有真正接上 Mpurse / Billdesk / Razorpay 等上游。
- 目前沒有完成 Biller 與 PayLedger 的落庫與查詢模型。

### 3. 目前最重要的業務語意

- callback success = recharge success
- 但這裡的 recharge success，只代表 payin 成功，也就是錢進到我方或合作方的收款流程中。
- 它不代表 user 的 bill 已經真的被支付完成。
- 真正的 bill payment / payout 半段，這個 POC 還沒寫完。

### 4. 整體模組圖

```text
+----------------------------+
| Client / App / 呼叫端      |
+----------------------------+
              |
              | 下單 / 發起支付
              v
+----------------------------+        +----------------------------+
| payin module               | -----> | Upstream PA Channels       |
| 收款入口 / callback 收口    |        | Mpurse / Billdesk / ...    |
| channel 選擇 / log / attempt|       | 目前多為 mock / placeholder|
+----------------------------+        +----------------------------+
              |
              | event / outbox
              v
+----------------------------+        +----------------------------+
| GCP Pub/Sub + Outbox       | -----> | payCore module             |
| 非同步事件橋接              |        | 總狀態機 / Saga 協調中心    |
+----------------------------+        +----------------------------+
                                                  |
                            +---------------------+---------------------+
                            |                                           |
                            | event / outbox                            | event / outbox
                            v                                           v
         +----------------------------+              +----------------------------+
         | biller module              |              | payLedger module           |
         | App 端帳單 / 顯示模型       |              | 後台帳務 / 報表 / 查詢模型  |
         | 提供 biller list / bill 查詢|              | 目前 consumer 殼已在       |
         | 接收 payCore 事件投影      |              +----------------------------+
         +----------------------------+

Client / App ........................................> biller module
                     初始 biller list / bill 查詢
```

### 5. 寫模型與讀模型的切分

```text
+------------------------------+    +----------------------------------+
| 核心交易寫模型                |    | 顯示與查詢讀模型                  |
| payin / payCore              |    | biller / payLedger               |
| 必須以 RDBMS 為主             |    | 目前規劃可先走 MySQL，再保留      |
| 重點是狀態正確與一致性         |    | 未來切 NoSQL 的彈性               |
+------------------------------+    +----------------------------------+
```

### 6. 對未來延伸的建議定位

雖然這個 POC 目前偏 biller 方向，但骨架其實可以延伸到:

- 一般支付。
- 遊戲支付。
- 其他需要「收款 -> 狀態協調 -> 事件分發 -> 後台展示」的支付場景。

如果之後要往多支付產品走，建議保留目前這個方向:

- payin / payout 負責上下游介接。
- payCore 負責總狀態機。
- biller / payLedger 或其他 read model 模組負責各自視角的投影與查詢。

---

## BLOCK 2: 重要技術的非技術版說明

### 1. 為什麼要拆成 payin、payCore、biller、payLedger

因為支付系統最怕的是「核心交易狀態」和「顯示、通知、報表、查詢」全部糾纏在一起。

這個 POC 的切法本質上是在做兩件事:

- 把真正會影響金流正確性的核心流程，盡量集中在 payin / payCore。
- 把 user 顯示面、後台報表、未來可能新增的副作用，切到事件之後再處理。

這樣的好處是:

- 核心交易流程比較穩。
- 未來新增讀模型時，不用一直去改核心流程。
- 當渠道數量變多時，變化主要集中在 adapter / factory，不會一直碰總流程。

### 2. PA 是什麼，為什麼目前沒有 PCI DSS 規劃

這個 POC 目前走的是 PA。

白話講，付款人填寫支付資料時，會被導到甲方或上游支付頁面，敏感支付資料不會直接經過我方 server，所以目前沒有把 PCI DSS 放進這份 POC 的規劃。

相對地，PG 是另一條路:

- 支付資料會真的進到我方 server。
- 那時候就不只是程式架構問題，還會牽涉 PCI DSS、資安、資料落點、稽核與隔離等問題。

目前這份 POC 還沒進入 PG 階段。

### 3. PayCore 為什麼要當總狀態機與 Saga 中心

因為支付系統最後最怕的是:

- 多模組各自判斷成功。
- 事件互相傳來傳去。
- 最後沒有一個地方可以明確說「這筆交易目前到底算什麼狀態」。

PayCore 在這個設計裡，就是那個最後要收口的地方。

白話講:

- payin 負責收錢。
- payout 未來負責對外付款或繳費。
- 但「整筆交易是否走到哪一階段」應該由 PayCore 統一判定。

這就是 Saga 協調者的角色。

### 4. Outbox 是什麼

Outbox 可以先理解成:

- 先把業務資料寫進資料庫。
- 同時把「等等要發出去的事件」也先寫進資料庫。
- 不直接在主流程裡賭 MQ 一定成功。
- 由背景程序再慢慢把事件送出去。

它要解的核心問題是:

- 如果 DB 寫成功了，但 MQ 發失敗怎麼辦。
- 如果主流程做到一半掛掉，事件到底算發了沒。

在支付系統，這種問題不能靠運氣，所以這份 POC 保留了 Outbox 這種比較保守、也比較容易追資料流的做法。

### 5. 樂觀鎖是什麼

樂觀鎖可以先理解成:

- 兩個人同時改同一筆交易狀態時，不讓後來的人把前面已經更新過的狀態蓋掉。

支付系統裡最怕狀態倒退，例如:

- 明明已經成功。
- 卻因為另一條慢到的流程，把狀態又寫回失敗或初始化。

所以有狀態機的核心表，通常都需要 version 欄位來保護。

### 6. 工廠與策略模式在這個 POC 裡的意義

這不是為了炫技，而是因為支付渠道一定會變。

今天可能是:

- Mpurse
- Billdesk
- Razorpay

明天可能又多別家。

如果每多一家都去改核心 usecase，最後核心流程一定會爛掉。用 factory / strategy 的目的，是把「變動很大」的渠道邏輯隔離出去。

### 7. 為什麼 read side 要切開

因為 Biller 跟 PayLedger 看的資料視角不同。

- Biller 比較偏 user facing。
- PayLedger 比較偏 admin / finance facing。

如果全部共用同一套查詢模型，最後只會把核心交易表搞成各種查詢妥協版。

所以這裡保留了讀寫分離的方向:

- 核心支付維持 RDBMS 與嚴格交易邏輯。
- 顯示側可以先用 MySQL。
- 以後如果查詢需求真的更彈性，再切 NoSQL 也比較容易。

### 8. 一致性做法比較

| 做法 | 白話理解 | 優點 | 代價 | 對這個 POC / 後續系統的判斷 |
| --- | --- | --- | --- | --- |
| Outbox | 事件先記帳，再由背景程序送出 | 資料流最容易追，適合支付核心 | 需要排程、outbox table、補償思維 | 這份 POC 保留它是合理的，尤其支付核心很需要看清每一步 |
| RocketMQ | 以 MQ 為中心做事件一致性 | 適合新 app 做事件驅動，吞吐與投遞能力好 | 會更依賴 MQ 與團隊操作能力 | 如果公司新 app 想統一走 RocketMQ，這是合理方向，但支付核心仍要非常清楚 DB 與狀態流向 |
| Seata | 幫多資源做分散式交易協調 | 可以做跨資源事務 | 較重、侵入性高、維運複雜 | 對支付核心要很謹慎，不建議在 POC 階段優先走這條 |

### 9. 為什麼這份 POC 用 GCP Pub/Sub

不是因為它一定是最終正式方案，而是因為這份 POC 的目標是先把:

- 模組切分
- 事件流
- outbox relay
- consumer 收口

這些骨架跑起來。

因此目前用 GCP Pub/Sub 當事件通道是合理的。但正式落地時，仍可以依公司後續基礎設施策略，評估要不要改成 RocketMQ 或其他正式方案。

### 10. Virtual Threads 在這個 POC 的位置

這個概念在這份文件不用展開太多。

目前你只要先知道:

- 設計上有把高併發 I/O 納入考量。
- 程式碼裡已有少量使用 Virtual Threads 的痕跡。
- 真正要不要大規模採用，接手後可再依實際壓測結果與團隊熟悉度決定。

---

## BLOCK 3: 目前程式碼的詳細介紹

### 1. 目前 repo 的技術事實

- pom.xml (line 8)
- pom.xml (line 30)
- pom.xml (line 51)
- pom.xml (line 55)
- pom.xml (line 64)
- pom.xml (line 83)
- src/main/resources/application.yml (line 8)
- src/main/resources/application.yml (line 13)
- src/main/resources/application.yml (line 18)
- src/main/resources/config/application-local.properties (line 1)
- src/main/resources/config/application-local.properties (line 4)
- src/main/resources/config/application-local.properties (line 9)
- firebase.json (line 2)
- firebase.json (line 10)

目前程式碼層面的事實如下:

- 技術堆疊是 Java 21 + Spring Boot 3.5.5。
- 核心依賴 JPA + PostgreSQL。
- repo 內有 Firestore 與 Pub/Sub 依賴。
- local profile 直接指向 PostgreSQL，且 ddl-auto 目前是 create-drop。
- firebase.json 內有 firestore 與 pubsub emulator 設定。
- 但 Spring 設定檔目前只明確啟用 Firestore emulator，沒有看到 Pub/Sub emulator host 的 Spring 設定。

這代表:

- 目前 Firestore 比較像保留中的能力，不是已落地的 read side。
- Pub/Sub 雖然有 emulator port 配置痕跡，但如果要完全本機化跑通，可能還需要再補 Spring 端設定。
- local properties 內含本機資料庫帳密，正式接手後建議改為環境變數或 secret 管理。

> 注意: 目前程式的核心 DB 是 PostgreSQL，不是 MySQL。你口頭規劃中的 MySQL，比較應該被理解成未來 read side 的落點方向，而不是這份 POC 的現況。

### 2. 啟動與基礎設施 wiring

- src/main/java/com/citrus/PaymentPocApplication.java (line 8)
- src/main/java/com/citrus/common/config/TxConfig.java (line 13)
- src/main/java/com/citrus/common/config/TransactionConfig.java (line 13)
- src/main/java/com/citrus/common/config/PubSubConfig.java (line 23)

目前應用啟動時，會做幾件重要的事情:

```text
Spring Boot 啟動
-> 掛上 JPA transaction manager
-> 掛上 TransactionTemplate
-> 啟用排程
-> 初始化 Pub/Sub topics / subscriptions
-> 啟動各個 consumer 的 @PostConstruct 訂閱
```

補充幾點:

- PubSubConfig 會在啟動時自動建立 topics / subscriptions。
- TxConfig 把 jpaTxManager 設成主要 transaction manager。
- TransactionConfig 額外準備了多種 TransactionTemplate，但目前實際用到的主要是預設 transactionTemplate。

### 3. 目前可見的 API 入口

- src/main/java/com/citrus/payin/controller/PayinController.java (line 23)
- src/main/java/com/citrus/payin/controller/CallbackController.java (line 29)
- src/main/java/com/citrus/payCore/controller/BbpsController.java (line 23)
- src/main/java/com/citrus/payin/object/req/InitiatePaymentReq.java (line 14)
- src/main/java/com/citrus/payCore/object/req/RechargeRetrySaveReq.java (line 12)

目前 repo 裡真正可打到的入口大概有三個:

1. /citrus/payin/pa
2. /citrus/callback/mpurse
3. /citrus/bbps/test

其中:

- /payin/pa 是主要入口。
- /callback/mpurse 是 callback 收口。
- /bbps/test 比較像開發測試用入口，用來手動塞一筆 RechargeRetry。

#### 3.1 /payin/pa 目前 request 欄位

| 欄位 | 目前語意 |
| --- | --- |
| userGid | 我方 user id |
| billGid | 我方系統內的 biller 唯一 id |
| refId | biller 官方唯一鍵 |
| actualPaymentAmount | 實際支付金額 |
| rechargeInfo | 充值資訊，未來可放 bill / vpa / 充值內容等 |

#### 3.2 關鍵 id 的定義

| 名稱 | 定義 |
| --- | --- |
| refId | biller 官方側唯一鍵 |
| billGid | 轉換到我方系統後的 biller 唯一 id |
| orderGid | 我方核心交易主單號 |
| rechargeGid | 我方 recharge 子單號 |

> 注意: 目前 billGid 會從 request 傳進 usecase 與 dto，但尚未真的落到 payin 或 payCore 的核心資料表中。

### 4. Payin init flow

- src/main/java/com/citrus/payin/usecase/store/PayinPaStoreUsecase.java (line 35)
- src/main/java/com/citrus/payin/event/PayinEvent.java (line 20)
- src/main/java/com/citrus/payin/usecase/store/PayinOutboxStoreUsecasae.java (line 21)
- src/main/java/com/citrus/payin/service/store/PayinOutboxStoreService.java (line 20)
- src/main/java/com/citrus/payin/usecase/store/PayinPaRouteUsecase.java (line 36)
- src/main/java/com/citrus/payin/factory/strategy/adapter/DefaultStrategy.java (line 19)
- src/main/java/com/citrus/payin/factory/channel/adapter/BilldeskChannel.java (line 21)
- src/main/java/com/citrus/payin/factory/channel/adapter/MpurseChannel.java (line 21)
- src/main/java/com/citrus/payin/service/store/PayinRouteStoreService.java (line 28)

目前 /payin/pa 這條線的實際流程如下:

```text
收到 /payin/pa
└─ PayinPaStoreUsecase.initiatePaPayment
   ├─ 產生 orderGid / rechargeGid
   ├─ 建 FirePaValidatedEvent
   │  └─ billAmount 目前先寫死 199.00
   ├─ firePaValidated
   │  └─ 寫入 payin_outbox，topic = INIT_EVENT
   ├─ executePa
   │  ├─ defaultStrategy 決定渠道順序
   │  ├─ 目前順序是 Billdesk -> Mpurse
   │  ├─ Billdesk mock 回傳失敗
   │  └─ Mpurse mock 回傳成功
   └─ savePaPayinBo
      ├─ 建 payin_record
      ├─ 建 payin_attempt
      ├─ 存 request log
      └─ 存 response log
```

這段流程有幾個重要現況:

- billAmount 目前不是從 biller 實際查回來，而是寫死 199.00。
- default strategy 現在只是示範 fallback 順序。
- Billdesk / Mpurse adapter 目前都只是 mock。
- 目前的設計是先 fire paycore init event，再去 call channel。

> 注意: PayinPaStoreUsecase 內有註解明寫，當前需求下不特別追求這一段的原子性。因此「寫 payin init outbox」與「呼叫 channel」目前不是同一個嚴格交易單位。

#### 4.1 payin_record / payin_attempt / payin_log 的角色

- src/main/java/com/citrus/payin/model/PayinRecord.java (line 27)
- src/main/java/com/citrus/payin/model/PayinAttempt.java (line 27)
- src/main/java/com/citrus/payin/service/store/PayinRouteStoreService.java (line 36)
- src/main/java/com/citrus/payin/service/store/PayinStoreService.java (line 63)

這三張表目前的分工很清楚:

- PayinRecord: 這筆外部收款交易的整體狀態。
- PayinAttempt: 每一次實際打渠道的嘗試。
- PayinLog: request / response / callback 的黑盒子紀錄。

如果要做支付交接，這種拆法是合理的，因為之後查問題時，會需要分清楚:

- 主單目前是什麼狀態。
- 嘗試過哪些渠道。
- 每一次 request / response 長什麼樣。

### 5. PayCore init flow

- src/main/java/com/citrus/payCore/consumer/PaycoreEventConsumer.java (line 31)
- src/main/java/com/citrus/payCore/object/dto/PayCoreInitDto.java (line 14)
- src/main/java/com/citrus/payCore/usecase/store/PayCoreStoreUsecase.java (line 32)
- src/main/java/com/citrus/payCore/service/store/PayCoreStoreService.java (line 29)
- src/main/java/com/citrus/payCore/model/BbpsOrder.java (line 27)
- src/main/java/com/citrus/payCore/model/BbpsPayment.java (line 27)
- src/main/java/com/citrus/payCore/model/BbpsRecharge.java (line 25)

當 payin 的 INIT_EVENT 被送到 Pub/Sub 後，PayCore consumer 會接起來，並且建立三張核心資料:

```text
PayCore 收到 INIT_EVENT
└─ createPaymentOrderInit
   ├─ 依 refId 查 bbps_order 是否已存在
   ├─ 不存在 -> 建 bbps_order
   ├─ 同步建 bbps_payment
   ├─ 同步建 bbps_recharge
   └─ firePaymentOrderInit
      └─ 目前只有 sysout，尚未延伸事件
```

這裡的設計意圖很明確:

- BbpsOrder = 整筆交易主狀態。
- BbpsRecharge = payin / recharge 半段。
- BbpsPayment = 未來真正 bill payment / payout 半段。

> 注意: 目前 duplicate refId 的 else 分支基本上還沒實作，只有註解。

### 6. Callback flow 與目前的語意

- src/main/java/com/citrus/payin/controller/CallbackController.java (line 29)
- src/main/java/com/citrus/payin/factory/callback/adapter/MpurseCallback.java (line 24)
- src/main/java/com/citrus/payin/service/query/PayinQueryService.java (line 29)
- src/main/java/com/citrus/payin/service/store/PayinStoreService.java (line 29)
- src/main/java/com/citrus/payin/event/PayinEvent.java (line 28)
- src/main/java/com/citrus/payCore/consumer/PaycoreEventConsumer.java (line 78)
- src/main/java/com/citrus/payCore/usecase/store/PayCoreStoreUsecase.java (line 50)
- src/main/java/com/citrus/payCore/event/PaycoreEvent.java (line 34)

目前 callback 的實際流程如下:

```text
收到 /callback/mpurse
└─ 直接讀 raw body 與 headers
   ├─ 用 MpurseCallback 解析 callback
   │  └─ 目前只吃出 refId
   ├─ 查 payin record + payin attempt
   ├─ 如果找不到
   │  └─ fireCallbackFailed
   │     └─ 目前只有 sysout，沒有真正落 retry 或 error table
   ├─ 如果找得到
   │  ├─ 先把 payin 狀態改成 CONFIRMING
   │  ├─ 存 callback log
   │  ├─ syncOrderStatus
   │  │  └─ 目前是 mock，再回一份成功資料
   │  ├─ updatePaCallbackBo
   │  │  └─ 把 payin record / attempt 改成 SUCCESS 或 FAILED
   │  └─ fireCallbackSucceeded
   │     └─ 寫入 payin_outbox，topic = CALLBACK_SUCCESS
   └─ PayCore 收到 CALLBACK_SUCCESS
      ├─ 依 refId 找 payCoreBo
      ├─ guard 狀態
      ├─ 把 BbpsOrder / BbpsRecharge 改成 RECHARGED
      └─ 再發 RECHARGE_SUCCESS
```

這裡一定要交接清楚的語意是:

- callback success = recharge success
- 這裡的 recharge success = payin 成功
- 不等於 bill 已支付完成

換句話說，目前的 RECHARGE_SUCCESS 是「錢進來了」的語意，不是「user 的 bill 已完成」。

### 7. Biller 與 PayLedger 的現況

- src/main/java/com/citrus/payLedger/consumer/LedgerEventConsumer.java (line 25)
- src/main/java/com/citrus/biller/concumer/BillerEventConsumer.java (line 25)
- src/main/java/com/citrus/share/enums/PubSubEnum.java (line 22)

RECHARGE_SUCCESS 目前會被 fan-out 到兩個 consumer:

- PAYLEDGER subscription
- BILLER subscription

但目前兩邊的 consumer 只做了:

- 收訊息
- 印 log
- ACK / NACK

目前都還沒有:

- read model 落庫
- projection 邏輯
- 查詢 API
- 去重 / 重放保護

所以這兩個模組現在只能算「事件入口殼已在」，還不能算已完成功能模組。

### 8. Outbox / Scheduler / Pub/Sub 的詳細做法

- src/main/java/com/citrus/common/model/Outbox.java (line 20)
- src/main/java/com/citrus/common/repository/PollingTaskRepository.java (line 21)
- src/main/java/com/citrus/payin/repository/PayinOutboxRepository.java (line 18)
- src/main/java/com/citrus/payCore/repository/PayCoreOutboxRepository.java (line 18)
- src/main/java/com/citrus/common/schedule/PollingTaskScheduler.java (line 50)
- src/main/java/com/citrus/common/publisher/abs/EventPublisherAbs.java (line 23)
- src/main/java/com/citrus/payin/schedule/PayinEventPublisher.java (line 18)
- src/main/java/com/citrus/payCore/schedule/PayCoreEventPublisher.java (line 18)

目前 Outbox 的 relay 流程如下:

```text
業務流程內寫 outbox
-> scheduler 每 10 秒掃一次
-> 用 claimPendingTasks 搶任務
-> 將 status 從 PENDING 改成 PROCESSING
-> 以 instanceId 查回自己搶到的資料
-> 找對應 TaskProcessor
-> 發到 Pub/Sub
-> 全部成功後把該 instanceId 批次改成 SUCCESS
```

幾個技術重點:

- scheduler 每次 batch 會產生一個 instanceId。
- repository claim SQL 用了 FOR UPDATE SKIP LOCKED。
- outbox 的 taskType 直接等於 eventType。
- processor 透過 getSupportedTypes 做 eventType 到 publisher 的映射。
- publisher 發送時會把 aggregateType、eventType、id 放進 Pub/Sub attributes。

> 注意: 目前 EventPublisherAbs 會同步等 publish future.get()，也就是發送成功才算成功。這是偏保守的設計，對支付核心是合理的，但之後要注意吞吐量與卡住時間。

### 9. RechargeRetry 補償機制

- src/main/java/com/citrus/payCore/event/PaycoreEvent.java (line 50)
- src/main/java/com/citrus/payCore/model/RechargeRetry.java (line 23)
- src/main/java/com/citrus/payCore/repository/RechargeRetryRepository.java (line 20)
- src/main/java/com/citrus/payCore/schedule/RechargeRetryProcessor.java (line 27)
- src/main/java/com/citrus/payCore/controller/BbpsController.java (line 23)

目前 RechargeRetry 這條線不是名詞而已，是真的有排程器在跑:

```text
PayCore 在 callback success 時找不到 order
-> fireOrderNotFound
-> 若 refId 尚未存在 retry table
-> 寫一筆 recharge_retry
-> scheduler 撿到後重新找 order
   ├─ 仍找不到 -> retryCount + 1，回 PENDING
   ├─ 超過 10 次 -> FAIL
   └─ 找到了 -> 直接補跑 handleRechargeSuccess，再刪除 retry 記錄
```

這個機制的意義是:

- callback 可能比 init event 更早到。
- 分散式事件本來就可能亂序。
- 所以 PayCore 必須接受「先收到 success，再慢一點才看到 order」這種情況。

> 注意: 目前 callback not found 分支是在 Payin 端 fireCallbackFailed，但那條線尚未真的落地補償。現在真正有落地的是 PayCore 這一側的 RechargeRetry。

### 10. 目前狀態機與 version 欄位

- src/main/java/com/citrus/payin/model/PayinRecord.java (line 50)
- src/main/java/com/citrus/payin/model/PayinAttempt.java (line 59)
- src/main/java/com/citrus/payCore/model/BbpsOrder.java (line 41)
- src/main/java/com/citrus/payCore/model/BbpsPayment.java (line 38)
- src/main/java/com/citrus/payCore/model/BbpsRecharge.java (line 36)
- src/main/java/com/citrus/payCore/service/guard/PayCoreGuardService.java (line 33)

目前有放 version 欄位的表:

- payin_record
- payin_attempt
- bbps_order
- bbps_payment
- bbps_recharge

這表示設計方向上，已經把 optimistic lock 納入考量。

但目前真正跑到的狀態，大致只有這些:

```text
PayinRecord
INIT -> CONFIRMING -> SUCCESS / FAILED

PayinAttempt
INITIATE_PAYMENT_SUCCESS or INITIATE_PAYMENT_FAIL
-> CONFIRMING
-> SUCCESS / FAILED

BbpsOrder
INIT -> RECHARGED

BbpsRecharge
INIT -> RECHARGED

BbpsPayment
INIT
```

而 enum 雖然已經定義了更多狀態，例如:

- RECHARGING
- PAYING
- PAID
- PAYMENT_FAILURE

但目前 runtime 還沒有真正走到那裡，很多對應 usecase / event 仍在註解或預留階段。

### 11. 工廠 / 策略 / adapter 的落地程度

- src/main/java/com/citrus/payin/factory/channel/PayinChannelFactory.java (line 10)
- src/main/java/com/citrus/payin/factory/callback/PayinCallbackFactory.java (line 7)
- src/main/java/com/citrus/payin/usecase/store/PayinPaRouteUsecase.java (line 31)

目前這套可擴充架構是成立的，但 adapter 還不是正式版本。

現況判讀:

- Factory 介面已建立。
- Strategy 選擇入口已建立。
- callback adapter 收口方式已建立。
- 但實際渠道串接細節仍是 mock。

接手後如果真的談定渠道，實際要補的會是:

- request mapping
- response mapping
- callback 驗簽
- idempotency
- timeout / retry / circuit breaker
- error code mapping
- channel SLA / fallback 策略

### 12. 目前已完成、半完成、未完成

#### 12.1 已完成的骨架

- payin init 入口。
- callback 收口入口。
- payCore 建單骨架。
- outbox 寫入與 scheduler relay。
- Pub/Sub topic / subscription 初始化。
- consumer 啟動與 ACK/NACK 模式。
- RechargeRetry 補償骨架。
- 核心表 version 欄位。

#### 12.2 半完成

- PayCore 總狀態機方向已明確，但 payment / payout 半段尚未接完。
- payin 的 factory / strategy / callback adapter 架構已經成形，但 adapter 內容仍是假資料。
- Biller / PayLedger consumer 已在，但還沒有 projection 落地。

#### 12.3 尚未完成

- 真實上游支付渠道串接。
- callback 驗簽與防重放。
- biller read model。
- payLedger read model。
- payout / bill payment 半段。
- 完整狀態遷移。
- 正式錯誤碼與 API 契約。
- 正式監控、告警、追蹤。
- 正式部署設定。
- 系統化測試案例。

### 13. 已知限制與風險

1. local profile 目前使用 create-drop，重啟會重建 schema，不適合任何接近正式環境的驗證。
2. local properties 直接寫死 DB 帳密，正式接手後要改 secret 管理。
3. billAmount 目前硬編 199.00，不是真正的 biller 查詢結果。
4. Biller / PayLedger 目前沒有真正資料落地，只有 consumer 殼。
5. callback not found 在 Payin 端沒有真的做持久化補償。
6. Pub/Sub emulator 雖然在 firebase.json 有配置，但 Spring 端尚未看到完整對應設定。
7. 現在的 RECHARGE_SUCCESS 很容易被誤會成 bill paid，實際上只是 payin success。
8. billGid 目前有進 request 流程，但尚未落核心資料表。
9. 多數 adapter 還是 mock，因此目前只能驗骨架，不能驗真正的金流整合。

### 14. 本機啟動與最小驗證方式

#### 14.1 啟動前提

- PostgreSQL 要先可用。
- 預設 local profile 會連 localhost:5432/postgres。
- 如果要跑 Firestore / PubSub emulator，可參考 firebase.json 的 port 設定。
- 但 Pub/Sub emulator 若要真的走本機，Spring 端可能還需要補設定。

#### 14.2 建議啟動順序

1. 啟動 PostgreSQL。
2. 如需 emulator，啟動 firebase emulators。
3. 啟動 Spring Boot app。
4. 觀察啟動 log，確認 topic / subscription 已建立、consumer 已 running。

local profile 預設 base URL:

- http://localhost:8082/citrus

#### 14.3 最小驗證範例

先打 /payin/pa:

```json
{
  "userGid": "user-001",
  "billGid": "bill-001",
  "refId": "ref-001",
  "actualPaymentAmount": 199.00,
  "rechargeInfo": "{\"billNo\":\"B001\",\"vpa\":\"demo@upi\"}"
}
```

再打 /callback/mpurse:

```json
{
  "refId": "ref-001"
}
```

目前 callback parser 實際只吃 refId，所以這樣就足以模擬整條流程。

#### 14.4 驗證時應觀察什麼

- payin_record 是否建立。
- payin_attempt 是否出現渠道 fallback 痕跡。
- 以目前預設策略來說，通常會看到一筆 Billdesk 失敗 attempt，再看到一筆 Mpurse 成功 attempt。
- payin_log 是否有 req / resp / callback 紀錄。
- payin_outbox 是否被 claim 並轉 SUCCESS。
- bbps_order / bbps_payment / bbps_recharge 是否建立。
- callback success 後，bbps_order / bbps_recharge 是否轉成 RECHARGED。
- pay_core_outbox 是否送出 RECHARGE_SUCCESS。
- Biller / PayLedger consumer 是否收到並 ACK。

#### 14.5 RechargeRetry 驗證方式

如果要單獨測補償，可打 /bbps/test:

```json
{
  "refId": "ref-retry-001"
}
```

接著觀察 recharge_retry 是否被 scheduler 撿起來重試。

### 15. 建議接手順序

如果由另一位 Java senior 接手，建議順序如下:

1. 先把 BLOCK 1 與 BLOCK 2 看完，確認業務語意。
2. 本機先打通 /payin/pa -> /callback/mpurse 這條主線。
3. 釐清 RECHARGE_SUCCESS 在目前系統中的語意，不要把它誤認成 bill paid。
4. 確認未來是先接真實 PA 渠道，還是先補 Biller / PayLedger read side。
5. 若先接渠道，優先補 adapter、驗簽、錯誤碼、idempotency。
6. 若先補核心一致性，優先補 callback not found 的持久化補償與完整狀態流。
7. 若要往正式化走，再評估 Pub/Sub、RocketMQ、Outbox 與部署策略的最終選型。

---

## 總結

這份 PaymentPoc 的價值，不在於它已經把支付完整做完，而在於它已經把支付系統最難收拾的幾件事先切出了骨架:

- 模組邊界。
- 總狀態機。
- 事件流。
- Outbox。
- 補償思路。
- 渠道擴充方式。

現在接手者真正要做的，不是推翻它，而是基於這個骨架，把:

- 真實渠道
- 真實 callback 規則
- 真實 read model
- 真實 payment / payout 半段

一段一段補上去。
